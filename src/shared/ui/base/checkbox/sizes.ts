import type { DiCheckboxSize } from './types'

export const CHECKBOX_SIZE_CLASSES: Record<DiCheckboxSize, string> = {
  xs: 'checkbox-xs',
  sm: 'checkbox-sm',
  md: 'checkbox-md',
  lg: 'checkbox-lg',
}
